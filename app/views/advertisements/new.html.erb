<h1>New Advertisement</h1>

<div class="row">
  <div class="col-md-4">
    <p>Here's tha deal...</p>
    <ul>
      <li>It can't make dollars if it doesn't make sense!</li>
      <li>That joke works waaay better spoken, lolz.</li>
      <li>Holla for dat dolla.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @advertisement do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter ad title" %>
      </div>
      <div class="form-group">
        <%= f.label :copy %>
        <%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter Copy..." %>
      </div>
      <div class="form-group">
        <%= f.label :price %>
        <%= f.text_field :price, class: 'form-control', placeholder: "$$$" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
